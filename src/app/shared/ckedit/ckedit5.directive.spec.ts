import { Ckedit5Directive } from './ckedit5.directive';

describe('Ckedit5Directive', () => {
  it('should create an instance', () => {
    const directive = new Ckedit5Directive();
    expect(directive).toBeTruthy();
  });
});
