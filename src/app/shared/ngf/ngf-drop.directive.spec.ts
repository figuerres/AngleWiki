import { NgfDropDirective } from './ngf-drop.directive';

describe('NgfDropDirective', () => {
  it('should create an instance', () => {
    const directive = new NgfDropDirective();
    expect(directive).toBeTruthy();
  });
});
