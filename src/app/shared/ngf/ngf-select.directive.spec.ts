import { NgfSelectDirective } from './ngf-select.directive';

describe('NgfSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new NgfSelectDirective();
    expect(directive).toBeTruthy();
  });
});
