import { NgfDirective } from './ngf.directive';

describe('NgfDirective', () => {
  it('should create an instance', () => {
    const directive = new NgfDirective();
    expect(directive).toBeTruthy();
  });
});
