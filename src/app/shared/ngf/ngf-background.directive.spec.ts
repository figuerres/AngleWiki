import { NgfBackgroundDirective } from './ngf-background.directive';

describe('NgfBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new NgfBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
