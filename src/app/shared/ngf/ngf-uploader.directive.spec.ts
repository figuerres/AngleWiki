import { NgfUploaderDirective } from './ngf-uploader.directive';

describe('NgfUploaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgfUploaderDirective();
    expect(directive).toBeTruthy();
  });
});
